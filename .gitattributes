# Saída de scripts é normalizada para LF, senão cada execução do pipeline
# gera um diff fantasma de arquivo inteiro no Windows (CRLF).
* text=auto eol=lf
*.png binary
*.pdf binary
*.joblib binary
*.parquet binary
